# Anchors pytest's rootdir at the repo root so conftest.py is loaded no matter
# which directory the suite is invoked from. Without a file like this, running
# pytest inside test/ makes test/ the rootdir, and the sys.path setup in the
# root conftest.py never runs.
[pytest]
testpaths = test
